# 🌓 Theme Switcher App

A simple React application demonstrating light and dark mode switching using Context API.
It includes a toggle button to change the theme globally and a card component styled with Tailwind CSS.
The selected theme dynamically updates the HTML root element, enabling full UI theme changes.

